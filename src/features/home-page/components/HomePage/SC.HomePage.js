import styled from 'styled-components'

const SCHomePage = styled.div`
  & .TopChart {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    margin-bottom: 3.9rem;
  }

  & .ReportTable {
    margin-top: 2rem;
  }
`

export default SCHomePage